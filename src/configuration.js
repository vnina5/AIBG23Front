
// export const API_ROOT = 'localhost:8081' // ovo je problem, radi kod kuce 
export const API_ROOT = 'localhost:1234' 

/*
export const API_ROOT = window.location.hostname === 'localhost' ? 'localhost:9080' : window.location.host;
export const API_ROOT = 'best1.aibg.best:9080'
ovde treba drugi server i port da gadjamo tj. endpoint <3
*/